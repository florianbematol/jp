{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "compile jp",
            "command": "make",
            "args": ["compile"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}